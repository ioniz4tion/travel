<h1><%= @city.name %></h1>

<div style="display:flex; width: 100%; justify-content: space-around">
	<div class="slide">
		<% @city.gen_images.each do |image| %>
		<div>
			<%= image_tag image.link %>
		</div>
		<% end %>
	</div>
</div>