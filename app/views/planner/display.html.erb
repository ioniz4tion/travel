City: <%= @package.name %>
<br/>
Hotel: <%= @package.gen_hotels.find_by_id(session[:hotel_id]).name %>
<br/>
Restaurant: <%= @package.gen_restaurants.find_by_id(session[:restaurant_id]).name %>
<br/>
Attraction: <% session[:attraction_id].each do |attraction| %> 
	<%= @package.gen_attractions.find_by_id(attraction).name %>
	<br/>
	<% end %>
<br/>