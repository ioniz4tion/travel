City: <%= @package.name %>
<br/>
Hotel: <%= @package.gen_hotels.find_by_id(session[:hotel_id]).name %>
<br/>
Restaurants: <% if session[:restaurant_id].is_a?(Array) %>
	<% session[:restaurant_id].each do |restaurant| %> 
	<%= @package.gen_restaurants.find_by_id(restaurant).name %>
	<br/>
	<% end %>
	<% else %>
	<%= @package.gen_restaurants.find_by_id(session[:restaurant_id]).name %>
	<% end %>
<br/>
Attractions: <% session[:attraction_id].each do |attraction| %> 
	<%= @package.gen_attractions.find_by_id(attraction).name %>
	<br/>
	<% end %>
<br/>